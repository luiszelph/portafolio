/**
 * Preferencia de tema que expone el **navegador** vía CSS
 * [`prefers-color-scheme`](https://developer.mozilla.org/es/docs/Web/CSS/@media/prefers-color-scheme):
 * refleja lo que el usuario tiene en el sistema o en los ajustes del propio navegador
 * (no hay otra API “de dispositivo” aparte de esta en la web estándar).
 */
export const PREFERS_COLOR_SCHEME_DARK_MEDIA = "(prefers-color-scheme: dark)" as const;
